# Deezer Playlist Printer

Generates a pdf file with thumbnails and music info (Title & Artist) for printing and art projects.

# Requirements

The program uses `fpdf` to create the pdf file.

Install with :

` pip install fpdf`


# Usage

`python3 main.py -i PLAYLIST_ID`
